# マテリアル

Renderer が描画する 3D 物体について、色とは別に**マテリアル (質感)** を指定できます。
すべての Renderer の共通プロパティとして、プロパティインスペクタの **Common**
セクションから設定します。

!!! note "レイトレーシング時のみ有効です"
    マテリアルはレンダリングウィンドウでのレイトレーシング出力に反映されます。
    分子ビュー (OpenGL のリアルタイム表示) では無視されます。

<!-- TODO(verify): Umbreon バックエンドが各マテリアルをどこまで反映するか
     (POV-Ray 専用の指定が残っていないか) を確認して追記する -->

<!-- TODO(screenshot): Umbreon での各マテリアルの比較。CueMol2 の比較画像は
     POV-Ray レンダリング (obsolete 予定) のため流用していない -->

## マテリアルの種類

マテリアルには、色指定が効くものと、マテリアル自体で色が決まっているものの
2 種類があります。

### 色指定が効くマテリアル

色を指定したうえで、その質感を変化させられるマテリアルです。

| 名前 | 質感 |
|---|---|
| `default` | 既定の質感。プラスチック光沢 |
| `shadow` | 「テカリ」のない影のような質感 |
| `nolighting` | 陰影付けなしのべた塗り |
| `matte` | つや消しの質感 |
| `spec_metal` | 非常に「テカリ」が強い金属質感 |
| `diff_metal` | つや消し金属質感 |
| `toon1` / `toon2` | トゥーン調の陰影 |

`shadow` はハイライト (テカリ) がないため、半透明にして他のものと重ねて表示するときに
向いています。

### 色指定ができないマテリアル

色の指定が無視されるタイプのマテリアルです。

| 名前 | 質感 |
|---|---|
| `metallic_chrome` | クロム金属光沢 |
| `metallic_copper` | 銅金属光沢 |
| `stone35` | 石のような模様・質感 |
| `wood31` | 木のような模様・質感 |

色を無視するマテリアル、特に金属光沢のものは周囲の環境光に影響されるため、
そのままでは暗くなることがあります。

## 指定方法

### Renderer 全体に対する指定

プロパティインスペクタの **Common** セクションの **Material** で指定します。
既定では空 (指定なし) で、この場合は `default` マテリアルが適用されます。

### 色ごとの指定

色を指定する際の[修飾子](../color-syntax.md#修飾子)としてマテリアルを指定できます。

```
<色指定>{material: <マテリアル名>}
```

色を黄色、マテリアルを `shadow` にする場合は次のように指定します
(色の指定方法が違うだけで、3 つとも同じ純色の黄色です)。

```
yellow{material:shadow}
rgb(1,1,0){material:shadow}
#FF0{material:shadow}
```

Coloring の Paint の各行に別々のマテリアルを指定すれば、
二次構造ごとに質感を変えるといったこともできます。

シート (sheet)
:   `SteelBlue{material:stone35}`

ヘリックス (helix)
:   `khaki{material:wood31}`

その他 (`*`)
:   `FloralWhite{material:metallic_chrome}`

色が無視されるマテリアルでも、色を指定しないとエラーになるため、
適当な色を指定してください。

!!! tip "模様の細かさ"
    `wood31` や `stone35` は木目や石の模様が細かいため、
    かなり拡大しないと模様が見えません。

## 関連項目

- [Renderer 共通プロパティ](common.md)
- [色の記法](../color-syntax.md)
- [エッジライン](edge-lines.md)
- [レンダリングウィンドウ](../../ui/rendering-window.md)

---

*最終確認: 2026-08-11 / 確認対象: 開発版 (tritium)*
